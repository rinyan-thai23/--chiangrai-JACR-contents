# JACRサイト制作状況

## 004 タイ産アルコールDB（2026-08-15再定義）

- 対象を「北タイのお酒」から「タイ国内で生産されるアルコール全般」へ変更。
- 本体は `tsv-knowledge/004.タイ産アルコール・地酒ブランド.tsv`（精査済み43件・25列）。
- ビール、スピリッツ、ワインを含む。輸入完成品は対象外。
- `レコード種別`、`親ブランド`、`タイ国内生産`、`国内生産確認根拠`を追加し、ブランドと個別商品を区別。
- `หมาใจดำ` はブランド行を設け、確認済みの個別商品を親子関係で収録。
- 27件版は `tsv-knowledge/004.タイのお酒・マニアック地酒_27件版.tsv` に保存。
- 旧50件版は `tsv-knowledge/004.北タイのお酒ブランド_旧50件監査.tsv` に隔離。
- 実在未確認名、酒銘柄ではない薬草処方、誤った産地説明は本体から除外。
- 不明な味・度数・原料は推測せず空欄。各行にタイ語Google画像検索URLと根拠URLを付与。
- 再生成スクリプトは `scripts/rebuild-thai-liquor-database.mjs`。
- 004サイトは `site/004-thai-alcohol/`。左一覧＋酒類絵文字フィルター、URLハッシュ切替、根拠資料・タイ語画像検索リンクを実装。
- サイト用データは `scripts/build-thai-alcohol-site-data.mjs` でTSVから再生成する。

## 005 タイの隠れた名産

- サイトは `site/005-thai-hidden-specialties/`。50件・9分類。
- 左サイドバーは全項目を日本語名で表示し、本文に原語名を併記。
- 分類フィルター、URLハッシュ切替、スマートフォンメニューを実装。
- サイト用データは `scripts/build-hidden-specialties-site-data.mjs` でTSVから再生成する。

## サイト全体

- 公開先はGitHub Pagesを想定した静的HTMLサイト。
- 入口は `site/index.html`。
- 現在のコレクションは `001-chiangrai-map` と `002-thai-history`。
- SEOより、シニアにも読みやすい文字サイズ、明確な日本語表示、スマートフォン対応を優先する。
- 公開画面では「DB」「データベース」「TSV」など制作元を意識させる表現を使用しない。

## 001 チェンライ地域案内（完成版）

- 場所：`site/001-chiangrai-map/`
- 28地域を1ページ内で切り替える構成。個別HTMLは作らない。
- 左サイドバー、地域絞り込み、Googleマップ、TSV由来の地域情報、見どころのGoogleマップリンクを実装済み。
- 01〜18はチェンライ県内、19〜28は県外として色分け。
- `chiangrai-guide.svg` は28地域共通のクリック可能な案内図。
- トップでは案内図を最上部に表示。各地域表示では同じSVGへ `#area-XX` を渡し、該当地域を黄色で強調する。
- SVGは枠・影・固定最小幅を持たず、画面幅に合わせて伸縮する。
- 地域配置は行政区画図と観光地図を参考に調整済み。簡略図であり正確な境界・距離は示さない。
- 001は一旦完成。大きな再設計は依頼があるまで行わない。

## 002 タイの歴史

- 場所：`site/002-thai-history/`
- `tsv-knowledge/002.タイの歴史.tsv` の100件を使用する。
- 左側は「時代区分を選択 → その時代の記事一覧」の二段階メニュー。
- 個別HTMLは作らず、同じページ内で内容を切り替える。
- トップは「タイの歴史」と9つの入口だけを表示する。
- 1話ずつ開く個別表示は廃止。時代区分ごとに10〜23話を一つの長いページへまとめ、縦スクロールで通読する。
- 時代ページ冒頭に全話目次を置き、各話を章として連続表示する。左側も「時代 → その時代の話一覧」の二段階メニューを維持する。
- 各時代は同じ `index.html` を使い、`?era=sukhothai` のようなクエリパラメータで直接開ける。入口は検索エンジンもたどれる通常のリンクにする。
- 派生データは `scripts/build-history-site-data.mjs` で `history-data.js` へ生成する。

## 共通の注意

- ユーザーの既存修正を維持する。
- TSV由来の空欄でない公開向けフィールドは、原則として画面に反映する。
- 管理用IDは画面へ表示しない。
- GitHub Pagesで動作するよう、外部ビルドを必要としない相対パス構成を守る。
- このプロジェクトではcanonicalを追加しない。

## 004 北タイのお酒ブランド（DB整備開始）

- 元データは `tsv-knowledge/004.北タイのお酒ブランド.tsv`、50件。
- 2026-08-15に9列から17列へ拡張し、全50件へタイ文字表記を追加した。
- 追加列：タイ文字表記、タイ文字表記区分、名称の要点、酒類大分類、おすすめの飲み方、製造者・ブランド、確認状態、出典URL。
- タイ文字は「公式表記確認」「一般的なタイ語名称」「暫定音写・要確認」を区別する。暫定音写を公式名称として公開しないこと。
- 既存の度数表記 `0.4` 等は `40%` 等へ直し、列名を `度数(ABV・目安)` に統一した。
- 現状は実在ブランド、一般酒名、地域の自家製酒らしき項目が混在する。確認状態が「実在・表記とも要追加調査」の行は、サイト制作前に個別調査または差し替えが必要。
- 再生成・修正用スクリプトは `scripts/expand-liquor-database.mjs`。
- タイ語検索による第1回精査後は22列。追加列は「タイ語検索判定」「推奨対応」「精査メモ」「Google画像検索URL」「公開用解説」「旧解説・ストーリー（未精査）」。
- 全50件に `https://www.google.com/search?tbm=isch&q=タイ文字検索語` 形式の画像検索URLを設定済み。サイトでは画像を保有せず、ボタンから検索結果を開く構成を想定する。
- 現時点の分類は、北タイ商品として確認3件、差し替え推奨13件、別枠・補足扱い11件、追加調査後に判断23件。
- 北タイ商品として確認済み：`หมาใจดำดอกไม้`、`หมาใจดำลำก้า`、`มานี ปิติ ชูใจ`。
- `เสือ 11 ตัว` と `ม้ากระทืบโรง` は酒銘柄ではなくヤードーンの薬材・処方名。`ข้าวหมาก` は発酵食品。`Kilo Spirits` と `White Rabbit Vodka` は北タイ外。全国銘柄も北タイ限定品と分ける。
- 差し替え推奨行の「公開用解説」は空欄にし、未精査の元文章は「旧解説・ストーリー（未精査）」へ隔離した。サイト側では旧解説を使用しないこと。

## 003 チェンライ名産50選

- 場所：`site/003-chiangrai-specialties/`
- `tsv-knowledge/003.チェンライ名産.tsv` の50件を使用する。
- 左サイドバーには50件を常時並べ、サイドバー内だけを縦スクロールできるようにする。
- PC版の名産詳細は、原則としてスクロールせず1画面で完結させる。
- ジャンルはタイトル右側に色付きタグとして表示する。
- 派生データは `scripts/build-specialties-site-data.mjs` で `specialties-data.js` へ生成する。
- 写真は `site/003-chiangrai-specialties/img/` に50件分を保存し、同フォルダの `IMAGE_SOURCES.tsv` で出典・作者・ライセンスを管理する。
- 正確な個別写真が確認できない項目には、誤認を避けて関連ジャンルの共通イメージを使用し、画面と台帳の両方でその旨を示す。
- 画像割り当ては `scripts/complete-specialty-image-set.mjs`、Wikimedia Commonsからの追加取得は `scripts/fetch-specialty-images.mjs` を使用する。
- 2026-08-15に1〜50の画像を再監査。以前の補完処理で生じた同一画像の再利用と明白な誤写真を解消し、JPEG 50件・SHA-256完全重複0件・出典URL欠落0件を確認した。
- 個別対象を確認できた写真と、固有商品の自由利用写真がなく対象分野の参考写真を使う項目を `IMAGE_SOURCES.tsv` の「画像区分」で区別する。
- 主な再修正対象は 004、009、012、017、022、024、026、028、031、032、039、042、044、045。取得処理は `scripts/replace-curated-specialty-images-batch2.mjs` に残してある。
- `complete-specialty-image-set.mjs` は旧式の共通画像補完を含むため、現画像一式に対して再実行しないこと。
